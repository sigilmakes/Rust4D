[package]
name = "rust4d_scripting"
version = "0.1.0"
edition = "2021"

[dependencies]
mlua = { version = "0.10", features = ["lua54", "vendored", "send"] }
log = "0.4"

[dev-dependencies]
env_logger = "0.11"
tempfile = "3"
